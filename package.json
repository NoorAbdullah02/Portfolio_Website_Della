{
  "name": "della-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "npm run dev --prefix frontend & npm run dev --prefix backend",
    "build": "npm run install:all && npm run db:push && npm run build --prefix frontend",
    "db:push": "npm run db:init --prefix backend"
  }
}